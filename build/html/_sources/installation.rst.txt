Installation
============

   After downloading this repo, you have to install dependencies just like any pipenv project.
   ``pipenv install``

   After this, you're ready to go and run:
   ``pipenv run python predom-color-py.py``